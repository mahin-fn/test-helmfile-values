name: Pluto
on:
  workflow_dispatch:

jobs:
  pluto:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Pluto
        uses: FairwindsOps/pluto/github-action@master
      
      - name: Use pluto
        run: |
          pluto detect-files -d deployment/charts
        